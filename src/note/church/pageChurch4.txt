"use client";

import { useState, useRef } from "react";
import Image from "next/image";
import { motion, useScroll, useTransform } from "framer-motion";
import { ArrowRight, Globe, ScrollText, Users2 } from "lucide-react";

// Assets
import churchMembers from "@/src/app/image/foto da igreja 1.jpg";
import photo1 from "@/src/app/image/foto da igreja 1.jpg";
import photo2 from "@/src/app/image/igreja/img_sobre_historia.png";
import photo3 from "@/src/app/image/igreja/manual.png";
import minister from "@/src/app/image/igreja/advice/pastor-emersn.jpg";
import official from "@/src/app/image/igreja/advice/rodrigo.jpg";
import official2 from "@/src/app/image/igreja/advice/ronaldo.jpg";

export default function SectionChurch() {
  const targetRef = useRef(null);
  const { scrollYProgress } = useScroll({ target: targetRef });
  
  // Efeito de movimento lateral no texto de fundo
  const x = useTransform(scrollYProgress, [0, 1], ["0%", "-50%"]);

  const sections = [
    { id: "01", title: "Nossa Origem", img: photo1, icon: <Globe size={14} />, tag: "Imbituba" },
    { id: "02", title: "IPB Nacional", img: photo2, icon: <ScrollText size={14} />, tag: "História" },
    { id: "03", title: "O Manual", img: photo3, icon: <Users2 size={14} />, tag: "Governo" },
  ];

  const council = [
    { name: "Emerson Baran", role: "Pastor", photo: minister },
    { name: "Rodrigo Pinho", role: "Presbítero", photo: official },
    { name: "Ronaldo Damazio", role: "Tesoureiro", photo: official2 },
  ];

  return (
    <section ref={targetRef} className="bg-white text-zinc-900 overflow-hidden font-sans">
      
      {/* 1. TEXTO DE FUNDO CINÉTICO */}
      <div className="sticky top-0 h-screen w-full flex items-center overflow-hidden pointer-events-none z-0">
        <motion.h2 
          style={{ x }}
          className="text-[25vh] font-black uppercase text-zinc-50 whitespace-nowrap leading-none"
        >
          Igreja Presbiteriana Imbituba Soli Deo Gloria Soli Deo Gloria
        </motion.h2>
      </div>

      {/* 2. CONTEÚDO SOBREPOSTO - HERO & INTRO */}
      <div className="relative z-10 -mt-[100vh]">
        <div className="min-h-screen flex flex-col justify-center px-6 md:px-24">
          <motion.div 
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            className="max-w-4xl"
          >
            <h1 className="text-6xl md:text-[120px] font-black tracking-tighter leading-[0.8] mb-12">
              FÉ QUE <br /> <span className="text-igreja-teal">ECOA.</span>
            </h1>
            <div className="grid md:grid-cols-2 gap-10">
              <p className="text-xl text-zinc-500 font-medium leading-tight uppercase italic">
                Uma jornada de compromisso bíblico no litoral de Santa Catarina.
              </p>
              <div className="h-px bg-zinc-200 w-full self-center" />
            </div>
          </motion.div>
        </div>

        {/* 3. GALERIA ASSIMÉTRICA DE HISTÓRIA */}
        <div className="px-6 md:px-24 py-40">
          <div className="grid grid-cols-1 md:grid-cols-12 gap-10">
            {sections.map((item, idx) => (
              <motion.div 
                key={idx}
                initial={{ opacity: 0, scale: 0.95 }}
                whileInView={{ opacity: 1, scale: 1 }}
                className={`relative flex flex-col ${
                  idx === 0 ? "md:col-span-7" : idx === 1 ? "md:col-span-5 mt-20" : "md:col-span-6 md:col-start-4"
                }`}
              >
                <div className="group relative overflow-hidden bg-zinc-100 rounded-sm">
                  <Image 
                    src={item.img} 
                    alt={item.title} 
                    className="w-full aspect-[16/10] object-cover transition-transform duration-1000 group-hover:scale-105"
                  />
                  <div className="absolute top-4 left-4 bg-white/90 backdrop-blur-sm px-3 py-1 flex items-center gap-2 rounded-full shadow-sm text-[10px] font-bold uppercase tracking-widest text-zinc-900">
                    {item.icon} {item.tag}
                  </div>
                </div>
                <div className="mt-6 flex justify-between items-end border-b border-zinc-100 pb-4">
                  <h3 className="text-3xl font-black uppercase tracking-tighter">{item.title}</h3>
                  <span className="font-mono text-zinc-300">[{item.id}]</span>
                </div>
              </motion.div>
            ))}
          </div>
        </div>

        {/* 4. O CONSELHO - LISTA HORIZONTAL "ART GALLERY" */}
        <div className="bg-zinc-950 text-white py-40 px-6 md:px-24 rounded-t-[100px] md:rounded-t-[200px]">
          <div className="mb-32">
            <h2 className="text-5xl md:text-8xl font-black uppercase tracking-tighter leading-none mb-6">
              O <span className="text-igreja-teal">Conselho</span>
            </h2>
            <p className="text-zinc-400 max-w-sm uppercase text-xs tracking-[0.3em]">Membros oficiais liderando a igreja local.</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-1 px-1 bg-zinc-800 border border-zinc-800">
            {council.map((person, idx) => (
              <div 
                key={idx} 
                className="relative bg-zinc-950 aspect-[3/4] group overflow-hidden"
              >
                <Image 
                  src={person.photo} 
                  alt={person.name} 
                  fill 
                  className="object-cover opacity-60 grayscale group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-700"
                />
                <div className="absolute inset-0 p-10 flex flex-col justify-end bg-gradient-to-t from-zinc-950 via-transparent to-transparent translate-y-20 group-hover:translate-y-0 transition-transform duration-500">
                  <span className="text-igreja-teal font-mono text-sm mb-2 italic tracking-widest">{person.role}</span>
                  <h4 className="text-3xl font-bold uppercase leading-none">{person.name}</h4>
                  <div className="mt-6 flex gap-4">
                    <div className="h-10 w-10 rounded-full border border-white/20 flex items-center justify-center hover:bg-white hover:text-black transition-colors cursor-pointer">
                      <ArrowRight size={16} className="-rotate-45" />
                    </div>
                  </div>
                </div>
                {/* Nome que aparece quando NÃO está em hover */}
                <div className="absolute bottom-10 left-10 group-hover:opacity-0 transition-opacity">
                   <h4 className="text-xl font-bold uppercase text-white/40 tracking-tighter">{person.name}</h4>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* 5. FOOTER DE IMPACTO */}
        <div className="py-60 flex flex-col items-center justify-center bg-white text-center">
           <div className="w-20 h-20 bg-igreja-teal rounded-full flex items-center justify-center mb-10 shadow-2xl shadow-igreja-teal/40">
              <ArrowRight size={32} className="text-white -rotate-90" />
           </div>
           <h3 className="text-4xl md:text-6xl font-black uppercase tracking-tighter max-w-2xl mb-12">
              Junte-se a nós no próximo <span className="italic font-serif font-light">Domingo.</span>
           </h3>
           <button className="px-10 py-5 bg-zinc-900 text-white rounded-full font-bold uppercase tracking-widest text-xs hover:bg-igreja-teal transition-all">
              Ver Localização no Maps
           </button>
        </div>
      </div>

    </section>
  );
}