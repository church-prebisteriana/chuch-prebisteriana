"use client";

import { useState, useRef } from "react";
import Image from "next/image";
import { motion, useScroll, useTransform } from "framer-motion";
import { ArrowRight, MoveDown, Compass, Scroll, Users } from "lucide-react";

// Assets
import churchMembers from "@/src/app/image/foto da igreja 1.jpg";
import photo1 from "@/src/app/image/foto da igreja 1.jpg";
import photo2 from "@/src/app/image/igreja/img_sobre_historia.png";
import photo3 from "@/src/app/image/igreja/manual.png";
import minister from "@/src/app/image/igreja/advice/pastor-emersn.jpg";
import official from "@/src/app/image/igreja/advice/rodrigo.jpg";
import official2 from "@/src/app/image/igreja/advice/ronaldo.jpg";

export default function SectionChurch() {
  const containerRef = useRef(null);
  const { scrollYProgress } = useScroll({ target: containerRef });

  // Efeito de Parallax suave para elementos de texto
  const textX = useTransform(scrollYProgress, [0, 1], ["0%", "-20%"]);

  const history = [
    { title: "A Cidade", id: "01", img: photo1, icon: <Compass /> },
    { title: "A Raiz", id: "02", img: photo2, icon: <Scroll /> },
    { title: "A Ordem", id: "03", img: photo3, icon: <Users /> },
  ];

  const council = [
    { name: "Emerson Baran", role: "Pastor", photo: minister },
    { name: "Rodrigo Pinho", role: "Presbítero", photo: official },
    { name: "Ronaldo Damazio", role: "Tesoureiro", photo: official2 },
  ];

  return (
    <section ref={containerRef} className="bg-[#f2f2f2] text-[#1a1a1a] selection:bg-igreja-teal selection:text-white">
      
      {/* 1. HERO - MINIMALISMO ABSOLUTO */}
      <div className="h-screen w-full flex flex-col justify-between p-6 md:p-12 relative overflow-hidden">
        <div className="flex justify-between items-start z-10">
          <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="text-xs font-mono tracking-widest uppercase">
            IPB Imbituba // Est. 19xx
          </motion.div>
          <div className="text-right text-[10px] leading-tight uppercase tracking-tighter text-zinc-400">
            Soli <br /> Deo <br /> Gloria
          </div>
        </div>

        <div className="z-10">
          <motion.h1 
            initial={{ y: 100, opacity: 0 }} 
            animate={{ y: 0, opacity: 1 }}
            className="text-[15vw] md:text-[12vw] font-black leading-[0.8] tracking-tighter italic"
          >
            COMU<span className="text-igreja-teal not-italic">NIDADE.</span>
          </motion.h1>
          <p className="mt-8 text-xl md:text-3xl max-w-2xl font-light leading-snug tracking-tight">
            Uma igreja centrada em Cristo, guiada pelas escrituras e enraizada em Imbituba.
          </p>
        </div>

        <motion.div animate={{ y: [0, 10, 0] }} transition={{ repeat: Infinity, duration: 2 }} className="flex gap-4 items-center font-mono text-xs uppercase tracking-widest">
          <MoveDown size={14} /> Role para explorar
        </motion.div>

        {/* Imagem de fundo flutuante */}
        <motion.div 
          style={{ opacity: useTransform(scrollYProgress, [0, 0.2], [0.15, 0]) }}
          className="absolute right-[-10%] bottom-[-10%] w-[60%] h-[70%] z-0"
        >
          <Image src={churchMembers} alt="Igreja" fill className="object-cover grayscale" />
        </motion.div>
      </div>

      {/* 2. TRANSIÇÃO TIPOGRÁFICA (TEXTO QUE SEGUE O SCROLL) */}
      <div className="py-20 border-y border-black/5 overflow-hidden whitespace-nowrap bg-white">
        <motion.div style={{ x: textX }} className="text-8xl md:text-[150px] font-black opacity-5 uppercase flex gap-20">
          <span>História Raízes Doutrina Família Fé</span>
          <span>História Raízes Doutrina Família Fé</span>
        </motion.div>
      </div>

      {/* 3. GALERIA "STAGGERED" (DESALINHADA) */}
      <div className="max-w-[1600px] mx-auto py-40 px-6 md:px-12">
        <div className="flex flex-col gap-40 md:gap-80">
          {history.map((item, idx) => (
            <div key={idx} className={`flex flex-col md:flex-row gap-12 items-center ${idx % 2 !== 0 ? 'md:flex-row-reverse' : ''}`}>
              <motion.div 
                initial={{ opacity: 0, scale: 0.9 }}
                whileInView={{ opacity: 1, scale: 1 }}
                viewport={{ once: true }}
                className="w-full md:w-1/2 aspect-[16/9] relative overflow-hidden rounded-sm group"
              >
                <Image src={item.img} alt={item.title} fill className="object-cover transition-transform duration-1000 group-hover:scale-110 grayscale group-hover:grayscale-0" />
                <div className="absolute inset-0 bg-igreja-teal/10 mix-blend-multiply opacity-0 group-hover:opacity-100 transition-opacity" />
              </motion.div>
              
              <div className="w-full md:w-1/3 flex flex-col gap-6">
                <span className="text-igreja-teal font-mono text-sm tracking-widest uppercase flex items-center gap-3">
                   {item.icon} {item.id}
                </span>
                <h2 className="text-6xl md:text-8xl font-bold tracking-tighter uppercase leading-none">{item.title}</h2>
                <p className="text-zinc-500 text-lg leading-relaxed italic">
                  Explorando a essência e o propósito de nossa existência em cada detalhe desta jornada ministerial.
                </p>
                <button className="flex items-center gap-4 text-xs font-black uppercase tracking-widest border-b border-black w-fit pb-2 hover:text-igreja-teal hover:border-igreja-teal transition-all">
                  Ler narrativa <ArrowRight size={14} />
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* 4. CONSELHO - "THE LIST" (ESTILO REBORD) */}
      <div className="bg-[#1a1a1a] text-white py-40 rounded-t-[100px]">
        <div className="max-w-7xl mx-auto px-6">
          <h2 className="text-[10vw] font-black uppercase tracking-tighter leading-none mb-32 border-b border-white/10 pb-10">
            Lide<span className="text-igreja-teal">rança.</span>
          </h2>

          <div className="space-y-0">
            {council.map((person, idx) => (
              <motion.div 
                key={idx}
                whileHover={{ backgroundColor: "rgba(255,255,255,0.03)" }}
                className="group relative border-b border-white/5 py-16 flex flex-col md:flex-row justify-between items-center transition-all px-8"
              >
                <div className="flex flex-col md:flex-row items-center gap-10 z-10">
                  <span className="font-mono text-zinc-600">0{idx + 1}</span>
                  <h3 className="text-4xl md:text-7xl font-bold uppercase tracking-tighter group-hover:italic transition-all">
                    {person.name}
                  </h3>
                </div>
                
                <div className="mt-4 md:mt-0 z-10">
                  <span className="text-igreja-teal font-mono uppercase tracking-[0.3em] text-sm">{person.role}</span>
                </div>

                {/* Foto que segue o cursor (ou aparece no hover) */}
                <div className="hidden md:block absolute right-[20%] top-1/2 -translate-y-1/2 w-48 h-64 opacity-0 group-hover:opacity-100 transition-all duration-500 scale-75 group-hover:scale-100 rotate-6 group-hover:rotate-0 pointer-events-none rounded-lg overflow-hidden border-4 border-white/20">
                   <Image src={person.photo} alt={person.name} fill className="object-cover" />
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </div>

      {/* 5. SEÇÃO FINAL - VAZIO E FOCO */}
      <div className="h-screen flex items-center justify-center bg-white">
        <div className="text-center px-6">
          <motion.div initial={{ scale: 0.8, opacity: 0 }} whileInView={{ scale: 1, opacity: 1 }} className="mb-10 inline-block p-6 border border-zinc-200 rounded-full">
            <Users className="text-igreja-teal" size={40} />
          </motion.div>
          <h2 className="text-5xl md:text-8xl font-black uppercase tracking-tighter leading-none mb-10">
            Venha <br /> Compartilhar <br /> <span className="italic">da Mesa.</span>
          </h2>
          <button className="px-16 py-6 bg-igreja-teal text-white font-black uppercase tracking-widest hover:bg-black transition-colors rounded-sm shadow-2xl">
            Localização e Horários
          </button>
        </div>
      </div>

    </section>
  );
}